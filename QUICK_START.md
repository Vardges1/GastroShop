# 🚀 Быстрый старт: Платежная система GastroShop

## Установка и запуск

### 1. Подготовка окружения
```bash
# Клонирование и переход в директорию
cd /Users/vardges/Desktop/GastroShop

# Установка зависимостей
cd apps/web && npm install
cd ../api && go mod download
```

### 2. Настройка базы данных
```bash
# Запуск PostgreSQL (через Docker)
docker-compose up -d postgres

# Применение миграций
cd apps/api
goose up
```

### 3. Конфигурация
```bash
# Копирование env файлов
cp apps/api/env.example apps/api/.env
cp apps/web/env.example apps/web/.env

# Настройка .env (уже настроен для mock режима)
# PAYMENT_PROVIDER=mock
# MOCK_WEBHOOK_SECRET=mock-webhook-secret-key
```

### 4. Запуск сервисов
```bash
# Backend (терминал 1)
cd apps/api
go run ./cmd/api

# Frontend (терминал 2)
cd apps/web
npm run dev
```

## 🎯 Демонстрация

### Автоматическая демонстрация
```bash
# Запуск полной демонстрации
./demo_payment_flow.sh
```

### Ручная демонстрация
1. **Админ панель**: http://localhost:3000/admin
2. **Создание заказа**: через API или фронтенд
3. **Mock checkout**: переход по checkout_url
4. **Тестирование**: кнопки "Успех"/"Отмена"

## 📊 Проверка работы

### API Health Check
```bash
curl http://localhost:8080/api/health
```

### Создание тестового платежа
```bash
curl -X POST http://localhost:8080/api/payments/create \
  -H "Content-Type: application/json" \
  -d '{"order_id": 1, "amount": 10000, "currency": "RUB"}'
```

### Проверка логов
```bash
# Логи API
docker-compose logs -f api

# Или если запущено локально
tail -f /var/log/gastroshop-api.log
```

## 🛠 Полезные команды

### Миграции БД
```bash
cd apps/api
goose up    # Применить миграции
goose down  # Откатить миграции
```

### Тестирование
```bash
# Backend тесты
cd apps/api && go test ./...

# Frontend тесты
cd apps/web && npm test
```

### Docker
```bash
# Полный запуск через Docker
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

## 📱 Основные URL

- **Главная**: http://localhost:3000
- **Админ панель**: http://localhost:3000/admin
- **Mock checkout**: http://localhost:3000/mock-checkout/{payment_id}
- **API**: http://localhost:8080/api
- **Health check**: http://localhost:8080/api/health

## 🎯 Сценарии для демонстрации

### 1. Успешная оплата
- Создать заказ → Создать платеж → Перейти на checkout → Нажать "Успех"

### 2. Отмена платежа
- Создать заказ → Создать платеж → Перейти на checkout → Нажать "Отмена"

### 3. Идемпотентность
- Отправить webhook несколько раз → Проверить логи

### 4. Админ панель
- Просмотр платежей → Ручная отправка webhook → Тестирование

## 🔧 Troubleshooting

### Проблемы с БД
```bash
# Проверка подключения
docker-compose exec postgres psql -U postgres -d gastroshop

# Пересоздание БД
docker-compose down -v
docker-compose up -d postgres
goose up
```

### Проблемы с API
```bash
# Проверка портов
lsof -i :8080
lsof -i :3000

# Перезапуск сервисов
docker-compose restart api
docker-compose restart web
```

### Проблемы с webhook
- Проверить подпись в логах
- Убедиться в правильности MOCK_WEBHOOK_SECRET
- Проверить формат JSON payload

---

## 🏆 Готово к демонстрации!

Система полностью настроена и готова для демонстрации на защите. Все компоненты работают корректно, есть полная трассировка операций и интерактивные инструменты для тестирования.
